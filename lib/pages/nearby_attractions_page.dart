import 'package:flutter/material.dart';

class NearbyAttractionsPage extends StatelessWidget {
  const NearbyAttractionsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Nearby Attractions page"),
    );
  }
}